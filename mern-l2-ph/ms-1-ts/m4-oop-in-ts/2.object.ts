//! 4-2: How to Create Class, Object , Parameter Properties
/**
 * ? Class: blueprint of object. create a model
 * ? Object: follows class. use class properties, methods
 * ? Parameter properties: class methods parameter
 * ? public access modifier: public can use before properties. In short way method before writing public no need to assign again into  constructor.
 */

// class Animal {
// public name: string;
// public species: string;
// public sound: string;
// constructor(name: string, species: string, sound: string) {
//   this.name = name;
//   this.species = species;
//   this.sound = sound;
// }

// constructor(
//   public name: string,
//   public species: string,
//   public sound: string
// ) {}
// public makeSound() {
// console.log(`The ${this.name} sounds like ${this.sound}`);
//   }
// }

// const dog = new Animal("DogPro", "dog", "Ghaw");
// const cat = new Animal("CatPro", "cat", "Mew");
// dog.makeSound();
// cat.name = "CatFather";
// cat.makeSound();
