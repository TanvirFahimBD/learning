//! 5_5 1: What is module ,different formats of module technique, import and export
// export const add = (param1: number, param2: number, param3: number): number => {
//   return param1 + param2 + param3;
// };

// export const sub = (param1: number, param2: number): number => {
//   return param1 - param2;
// };

// export const multiply = (param1: number, param2: number): number => {
//   return param1 * param2;
// };

// export const divide = (param1: number, param2: number): number => {
//   return param1 / param2;
// };

// const average = (param1: number, param2: number): number => {
//   return (param1 + param2) / 2;
// };

// // export default average;
// export default { add, sub, multiply, average };
