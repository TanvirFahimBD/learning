//! array type
/**
 * !string of array
 */
// const names: string[] = ["fahim", "tanvir"];
// console.log(names);
// names.map((name) => console.log(name.toUpperCase()));

/**
 * !number of array
 */
// const rollNumbers1: number[] = [34, 45];
// rollNumbers1[0] = 455;
// console.log(rollNumbers1);
// rollNumbers1.map((rollNum) => console.log(rollNum.toString()));

/**
 * !union type
 */
// const names = ["abul", "kabul", 43, 54, false];
// names.push(3);
// console.log(names);

/**
 * !tuple: strictly declare type not type changeable
 * during key value use-case apply tuple
 */
// const user: [number, string] = [45, "fahim"];
// console.log(user);
